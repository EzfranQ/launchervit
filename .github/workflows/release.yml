name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "LauncherVit-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/LauncherVit-source LauncherVit-${{ env.VERSION }}
          mv LauncherVit-Linux-Qt6-Portable*/LauncherVit-portable.tar.gz LauncherVit-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv LauncherVit-Linux-aarch64-Qt6-Portable*/LauncherVit-portable.tar.gz LauncherVit-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv LauncherVit-*.AppImage/LauncherVit-*-x86_64.AppImage LauncherVit-Linux-x86_64.AppImage
          mv LauncherVit-*.AppImage.zsync/LauncherVit-*-x86_64.AppImage.zsync LauncherVit-Linux-x86_64.AppImage.zsync
          mv LauncherVit-*.AppImage/LauncherVit-*-aarch64.AppImage LauncherVit-Linux-aarch64.AppImage
          mv LauncherVit-*.AppImage.zsync/LauncherVit-*-aarch64.AppImage.zsync LauncherVit-Linux-aarch64.AppImage.zsync
          mv LauncherVit-macOS*/LauncherVit.zip LauncherVit-macOS-${{ env.VERSION }}.zip
          mv LauncherVit-macOS*/LauncherVit.dmg LauncherVit-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf LauncherVit-${{ env.VERSION }}.tar.gz LauncherVit-${{ env.VERSION }}

          for d in LauncherVit-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="LauncherVit-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv LauncherVit-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in LauncherVit-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="LauncherVit-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv LauncherVit-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in LauncherVit-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="LauncherVit-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv LauncherVit-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: LauncherVit ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            LauncherVit-Linux-x86_64.AppImage
            LauncherVit-Linux-x86_64.AppImage.zsync
            LauncherVit-Linux-aarch64.AppImage
            LauncherVit-Linux-aarch64.AppImage.zsync
            LauncherVit-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            LauncherVit-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            LauncherVit-Windows-MinGW-w64-${{ env.VERSION }}.zip
            LauncherVit-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            LauncherVit-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            LauncherVit-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            LauncherVit-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            LauncherVit-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            LauncherVit-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            LauncherVit-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            LauncherVit-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            LauncherVit-Windows-MSVC-${{ env.VERSION }}.zip
            LauncherVit-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            LauncherVit-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            LauncherVit-macOS-${{ env.VERSION }}.zip
            LauncherVit-macOS-${{ env.VERSION }}.dmg
            LauncherVit-${{ env.VERSION }}.tar.gz
